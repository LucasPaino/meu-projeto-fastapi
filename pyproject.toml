[project]
name = "meu-projeto-fastapi"
version = "0.1.0"
description = "Projeto FastAPI com CI/CD"
authors = [
    {name = "Pedro",email = "pedro@silva.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi (>=0.133.1,<0.134.0)",
    "uvicorn (>=0.41.0,<0.42.0)",
    "pytest (>=9.0.2,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
